services:
  db:
    image: mariadb:lts-ubi9
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: Abc123
    ports:
      - "3306:3306"
    volumes:
      - dbVolume:/var/lib/mysql:Z

volumes:
  dbVolume:
